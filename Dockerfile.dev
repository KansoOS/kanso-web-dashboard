# syntax=docker/dockerfile:1.7
# Image de dev : dépendances installées au build, code monté en volume au run (hot-reload Vite).
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --no-audit --no-fund
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
